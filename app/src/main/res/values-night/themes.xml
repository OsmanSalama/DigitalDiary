<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DigitalDiaryApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <!-- Custom theme for your specific activity -->
    <style name="Theme.MainActivity" parent="Base.Theme.DigitalDiaryApp">
        <item name="android:background">@color/gruvbox_light_fg</item>

    </style>

    <!-- Custom theme for your specific activity -->
    <style name="Theme.NoteWritingActivity" parent="Base.Theme.DigitalDiaryApp">
        <item name="android:background">@color/gruvbox_light_fg</item>
    </style>

    <!-- Custom theme for your specific activity -->
    <style name="Theme.CalenderViewActivity" parent="Base.Theme.DigitalDiaryApp">
        <item name="android:background">@color/gruvbox_light_fg</item>
    </style>

    <!-- Custom theme for your specific activity -->
    <style name="Theme.SettingsViewActivity" parent="Base.Theme.DigitalDiaryApp">
        <item name="android:background">@color/gruvbox_light_fg</item>
    </style>
</resources>